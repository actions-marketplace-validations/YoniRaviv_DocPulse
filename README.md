# DocPulse

## Status

Phase 1 complete — `docpulse index` works on a 3-language fixture (Python/TypeScript/C#);
heuristic linking covers symbol-mentioning docs; symbol-free docs require embedding
linking (content-hash cache + `--heuristics-only` escape hatch in place).
