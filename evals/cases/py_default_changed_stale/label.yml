status: stale
intent: "Increase the default connection timeout from 30 s to 60 s."
reference_correction: |
  `connect(host, timeout=60)` opens a TCP connection to `host`.

  The `timeout` parameter sets the socket timeout in seconds; it defaults to **60**.
  If the server does not respond within that window the call raises `TimeoutError`.
